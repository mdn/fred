name: Main Review Companion

on:
  push:
    branches:
      - main-review-companion

  schedule:
    - cron: "0 0 * * *"

  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/_deploy.yml
    secrets: inherit
    with:
      prefix: fred
